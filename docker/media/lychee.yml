version: '3'
services:
  # lychee:
  #   container_name: lychee
  #   image: ghcr.io/linuxserver/lychee
  #   networks:
  #     - default
  #     - data
  #   depends_on:
  #     - mariadb
  #   environment:
  #     - PGID=${PGID}
  #     - PUID=${PUID}
  #     - TZ=${TIMEZONE}
  #     - DB_HOST=mariadb
  #     - DB_DATABASE=lychee
  #     - DB_USERNAME=lychee
  #     - DB_PASSWORD=${DB_PASS_LYCHEE}
  #   volumes:
  #     - ${NAS}/docker/lychee/config:/config
  #     - ${NAS}/docker/lychee/pictures:/pictures
  #     - ${NAS}:${NAS}:ro
  #   labels:
  #     namespace: media
  #     host: pics2.${SERVER_DOMAIN}
  #     port: 80

  lychee-all:
    image: lycheeorg/lychee
    container_name: lychee-all
    volumes:
      - ${NAS}/pictures/Organized:${NAS}/pictures/Organized:ro
      - ${NAS}/docker/lychee-all/conf:/conf
      - ${NAS}/docker/lychee-all/uploads:/uploads
      - ${NAS}/docker/lychee-all/sym:/sym
    networks:
      - data
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      # PHP timezone e.g. PHP_TZ=America/New_York
      - PHP_TZ=${TIMEZONE}
      - TIMEZONE=${TIMEZONE}
      #- APP_NAME=Laravel
      #- APP_ENV=local
      - APP_FORCE_HTTPS=true
      #- APP_DEBUG=true
      - APP_URL=https://pics-all.w00t.cloud
      #- LOG_CHANNEL=stack
      - DB_CONNECTION=mysql
      - DB_HOST=mariadb
      - DB_PORT=3306
      - DB_DATABASE=lychee-all
      - DB_USERNAME=lychee-all
      - DB_PASSWORD=${DB_PASS_LYCHEE}
      #- DB_DROP_CLEAR_TABLES_ON_ROLLBACK=false
      #- DB_OLD_LYCHEE_PREFIX=''
      #- BROADCAST_DRIVER=log
      #- CACHE_DRIVER=file
      #- SESSION_DRIVER=file
      #- SESSION_LIFETIME=120
      #- QUEUE_DRIVER=sync
      #- SECURITY_HEADER_HSTS_ENABLE=false
      #- REDIS_HOST=127.0.0.1
      #- REDIS_PASSWORD=null
      #- REDIS_PORT=6379
      #- MAIL_DRIVER=smtp
      #- MAIL_HOST=smtp.mailtrap.io
      #- MAIL_PORT=2525
      #- MAIL_USERNAME=null
      #- MAIL_PASSWORD=null
      #- MAIL_ENCRYPTION=null
      #- PUSHER_APP_ID=''
      #- PUSHER_APP_KEY=''
      #- PUSHER_APP_SECRET=''
      #- PUSHER_APP_CLUSTER=mt1
      - STARTUP_DELAY=30
    labels:
      namespace: media
      host: pics-all.${SERVER_DOMAIN}
      port: 80

  lychee:
    image: lycheeorg/lychee
    container_name: lychee
    volumes:
      - ${NAS}/ripper/ichive:/media:ro
      - ${NAS}/docker/lychee-test/conf:/conf
      - ${NAS}/docker/lychee-test/uploads:/uploads
      - ${NAS}/docker/lychee-test/sym:/sym
    networks:
      - data
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      # PHP timezone e.g. PHP_TZ=America/New_York
      - PHP_TZ=${TIMEZONE}
      - TIMEZONE=${TIMEZONE}
      #- APP_NAME=Laravel
      #- APP_ENV=local
      - APP_FORCE_HTTPS=true
      #- APP_DEBUG=true
      - APP_URL=https://pics2.w00t.cloud
      #- LOG_CHANNEL=stack
      - DB_CONNECTION=mysql
      - DB_HOST=mariadb
      - DB_PORT=3306
      - DB_DATABASE=lychee-test
      - DB_USERNAME=lychee-test
      - DB_PASSWORD=${DB_PASS_LYCHEE}
      #- DB_DROP_CLEAR_TABLES_ON_ROLLBACK=false
      #- DB_OLD_LYCHEE_PREFIX=''
      #- BROADCAST_DRIVER=log
      #- CACHE_DRIVER=file
      #- SESSION_DRIVER=file
      #- SESSION_LIFETIME=120
      #- QUEUE_DRIVER=sync
      #- SECURITY_HEADER_HSTS_ENABLE=false
      #- REDIS_HOST=127.0.0.1
      #- REDIS_PASSWORD=null
      #- REDIS_PORT=6379
      #- MAIL_DRIVER=smtp
      #- MAIL_HOST=smtp.mailtrap.io
      #- MAIL_PORT=2525
      #- MAIL_USERNAME=null
      #- MAIL_PASSWORD=null
      #- MAIL_ENCRYPTION=null
      #- PUSHER_APP_ID=''
      #- PUSHER_APP_KEY=''
      #- PUSHER_APP_SECRET=''
      #- PUSHER_APP_CLUSTER=mt1
      - STARTUP_DELAY=30
    labels:
      namespace: media
      host: pics2.${SERVER_DOMAIN}
      port: 80
